#!/bin/bash
#
# Usage
#   redmine list [options] [pjspec...]
#
# Description
#   Print task list to standard output. If you give pjspec as a parameter,
#   the tasks belonging to the specified project are printed.
#   If you give no parameter, tasks of all projects are printed.
#
# Options
#   -a user_id    filter ticket with given user (user ID or "me")
#   -c            show closed tickets
#   -C            hide closed tickets
#   -g            grouping by projects
#   -G            no grouping
#   -h            show this message

show_help() {
	sed -n 2,$[$BASH_LINENO-4]p $BASH_SOURCE | grep "^#" | sed 's/^#/ /'
}

PROJECT=all
GROUPING=false
COLOR=true
while [[ $# -gt 0 ]] ; do
	key="$1"
	case $key in
		-a)
			ASSIGNED="$2"
			ASSIGNED_OPT="&assigned_to_id=$ASSIGNED"
			shift 2
			;;
		-c)
			RM_SHOW_CLOSED=true
			shift 1
			;;
		-C)
			RM_SHOW_CLOSED=false
			shift 1
			;;
		-g)
			GROUPING=true
			shift 1
			;;
		-G)
			GROUPING=false
			shift 1
			;;
		-h)
			show_help
			exit 0
			;;
		*)
			# echo "invalid option $key"
			break
			;;
	esac
done

[ ! -t 1 ] && COLOR=false
[ "$#" -gt 0 ] && PROJECT="$@"

update_local_cache > /dev/null 2>&1 || exit 1

PROJECTS="$(pjspec_to_pjid $PROJECT | tr ' ' ',')" SHOWCLOSED=$RM_SHOW_CLOSED COLOR=$COLOR GROUPING=$GROUPING python3 $THISDIR/use_json.py $RM_CONFIG/issues.json > $TMPDIR/out
cat $TMPDIR/out | column -t -s $'\t'
